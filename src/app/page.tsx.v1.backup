const categories = [
  { icon: "⚡", name: "Electrician" },
  { icon: "🔧", name: "Plumber" },
  { icon: "🔌", name: "Generator" },
  { icon: "❄️", name: "AC repair" },
  { icon: "🧵", name: "Tailor" },
  { icon: "🧹", name: "Cleaner" },
];

export default function Home() {
  return (
    <main className="mx-auto max-w-md px-5 pb-16 md:max-w-5xl">
      <nav className="flex items-center justify-between py-6 fa-rise">
        <div className="flex items-center gap-2.5">
          <div className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#0F6E56] text-sm font-bold text-[#E4F5EE] shadow-md">
            F
          </div>
          <span className="text-lg font-semibold tracking-tight">FindAm</span>
        </div>
        <span className="text-sm text-[#5A6B63]">Port Harcourt</span>
      </nav>

      <section className="pt-8 md:pt-16 md:text-center">
        <p className="fa-rise text-xs font-semibold uppercase tracking-[0.18em] text-[#0F6E56]" style={{ animationDelay: "0.05s" }}>
          Port Harcourt&apos;s trusted hands
        </p>
        <h1 className="fa-serif fa-rise mt-3 text-4xl leading-[1.15] md:text-6xl md:leading-[1.1]" style={{ animationDelay: "0.12s" }}>
          Find someone you can trust,
          <span className="text-[#0F6E56]"> in two minutes.</span>
        </h1>
        <p className="fa-rise mt-4 text-[15px] leading-relaxed text-[#5A6B63] md:mx-auto md:max-w-xl md:text-lg" style={{ animationDelay: "0.2s" }}>
          Every artisan vetted in person. Real reviews from real neighbours. No
          stories, no fear.
        </p>

        <div className="fa-input fa-rise mt-8 flex items-center gap-3 px-5 py-4 md:mx-auto md:max-w-xl" style={{ animationDelay: "0.28s" }}>
          <span className="text-[#9AA8A1]">🔍</span>
          <span className="text-[15px] text-[#9AA8A1]">
            What do you need? Try &quot;AC repair&quot;
          </span>
        </div>
      </section>

      <section className="mt-10 md:mt-16">
        <div className="grid grid-cols-2 gap-3 md:grid-cols-3">
          {categories.map((c, i) => (
            <div
              key={c.name}
              className="fa-tile fa-rise flex cursor-pointer items-center gap-3 px-4 py-4"
              style={{ animationDelay: `${0.32 + i * 0.06}s` }}
            >
              <span className="text-xl">{c.icon}</span>
              <span className="text-sm font-medium">{c.name}</span>
            </div>
          ))}
        </div>
      </section>

      <section className="mt-12 md:mt-20">
        <h2 className="fa-serif text-2xl md:text-center md:text-3xl">
          Top rated near you
        </h2>
        <div className="fa-surface mt-5 p-5 md:mx-auto md:max-w-xl">
          <div className="flex items-center gap-3.5">
            <div className="flex h-12 w-12 items-center justify-center rounded-full bg-[#E4F5EE] text-sm font-bold text-[#0A4A3A]">
              CE
            </div>
            <div className="min-w-0 flex-1">
              <div className="flex items-center gap-2">
                <span className="font-semibold">Chinedu Eze</span>
                <span className="fa-badge">✓ Verified</span>
              </div>
              <p className="mt-0.5 text-[13px] text-[#5A6B63]">
                Electrician · Rumuola, GRA, Woji
              </p>
            </div>
          </div>
          <div className="mt-3 flex items-center gap-4 text-[13px] text-[#5A6B63]">
            <span className="text-[#B78A2E]">★ 4.9 (31)</span>
            <span>84 jobs done</span>
            <span>~25 min reply</span>
          </div>
          <button className="fa-btn mt-4 w-full text-[15px]">
            Chat on WhatsApp
          </button>
        </div>
      </section>

      <footer className="mt-14 flex justify-between border-t border-[rgba(20,32,27,0.08)] pt-6 text-xs text-[#9AA8A1] md:mt-24">
        <span>Vetted in person</span>
        <span>Real reviews only</span>
        <span>Free for you, always</span>
      </footer>
    </main>
  );
}